# Izak Portfolio — Motion Polish Build

Fresh polished version with:

- animated black intro screen
- editorial hero reveal
- subtle cursor-responsive parallax
- animated film grain
- soft vignette
- improved typography hierarchy
- refined navigation tracking
- brighter metadata readability
- project row hover interactions
- profile image and neon circle assets retained
- no package-lock.json

## Vercel

Leave settings on default, or use:

- Install Command: `npm install --no-package-lock --no-audit --no-fund`
- Build Command: `npm run build`
- Output Directory: leave empty
